{
  "name": "carecore",
  "version": "1.0.0",
  "description": "CareCore medical platform - backend entrypoint for Railway",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
